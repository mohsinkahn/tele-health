/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    // outputFileTracingRoot: import.meta.dirname,
      outputFileTracingRoot: "D:/check/htdocs/Tele-health-crm",


};

export default nextConfig;
